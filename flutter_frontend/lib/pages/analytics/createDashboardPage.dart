import 'package:flutter/material.dart';

class CreateDashboardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create Dashboard Page')),
      body: Center(child: Text('Welcome to Create Dashboard Page')),
    );
  }
}
