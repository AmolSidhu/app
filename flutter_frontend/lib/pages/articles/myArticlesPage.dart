import 'package:flutter/material.dart';

class MyArticlesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My Articles Page')),
      body: Center(child: Text('Welcome to My Articles Page')),
    );
  }
}
