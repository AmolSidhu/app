import 'package:flutter/material.dart';

class SearchArticlesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search Articles Page')),
      body: Center(child: Text('Welcome to Search Articles Page')),
    );
  }
}
