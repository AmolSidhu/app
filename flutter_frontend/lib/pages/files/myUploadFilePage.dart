import 'package:flutter/material.dart';

class MyUploadFilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My Upload File Page')),
      body: Center(child: Text('Welcome to My Upload File Page')),
    );
  }
}
