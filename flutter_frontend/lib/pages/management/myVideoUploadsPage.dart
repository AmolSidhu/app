import 'package:flutter/material.dart';

class MyVideoUploadsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My Video Uploads')),
      body: Center(child: Text('Welcome to My Video Uploads Page')),
    );
  }
}
