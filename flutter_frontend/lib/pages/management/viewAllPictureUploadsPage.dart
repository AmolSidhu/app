import 'package:flutter/material.dart';

class ViewAllPictureUploadsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View All Picture Uploads')),
      body: Center(child: Text('This is the View All Picture Uploads Page')),
    );
  }
}
