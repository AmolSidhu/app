import 'package:flutter/material.dart';

class ViewAllArtistsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('All Artists')),
      body: Center(child: Text('List of all artists will be displayed here')),
    );
  }
}
