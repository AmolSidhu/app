import 'package:flutter/material.dart';

class PictureSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Picture Search Page')),
      body: Center(child: Text('Welcome to Picture Search Page')),
    );
  }
}
