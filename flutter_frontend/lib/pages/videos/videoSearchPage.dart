import 'package:flutter/material.dart';

class VideoSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Video Search Page')),
      body: Center(child: Text('Welcome to Video Search Page')),
    );
  }
}
